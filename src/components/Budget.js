import React from 'react';
//import { AppContext } from '../context/AppContext';

const Budget = () => {

    //const { budget} = useContext(AppContext);

    return (
        <div className='alert alert-success'>
            <span>Budget: $6000</span>
        </div>
    );
};

export default Budget;