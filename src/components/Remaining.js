import React from 'react'

const Remaining = () => {
    return (
        <div className='alert alert-primary'>
            <span>Remaining: $3000</span>
        </div>
    );
};

export default Remaining